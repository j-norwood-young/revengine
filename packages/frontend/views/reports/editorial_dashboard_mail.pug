extends ../email_layout

block content
    .container
        #App
block scripts
    if apikey && apiserver
        script.
            var user_id = "#{user.data._id}";
            var apikey = "#{apikey}";
            var apiserver = "#{apiserver}";
    if (dev)
        script(src="/assets/vue.js") 
    else
        script(src="/assets/vue.min.js")
    script.
        const app = new App();