.form-group
    label= field.name
    if data[field.key]
        for d, x in data[field.key]
            textarea.form-control.code(name=`${field.key}[]` rows="50")
                = d
    else
        textarea.form-control.code(name=`${field.key}[]` rows="50")